# Local-only project identifier used by the disposable CI stack.
project_id = "fantasyfootball_migration_ci"

[db]
major_version = 15

[auth]
site_url = "http://127.0.0.1:3000"
additional_redirect_urls = ["http://127.0.0.1:3000/**"]

[auth.email]
enable_signup = true
enable_confirmations = true
